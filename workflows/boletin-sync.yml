name: Boletín Minería — Sync Diario

on:
  schedule:
    - cron: '30 13 * * 1-5'  # 11:00 CLT (15:00 UTC), lunes–viernes
  workflow_dispatch:           # trigger manual desde GitHub Actions UI

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger sync endpoint
        run: |
          curl -sf -X GET \
            "${{ secrets.NEXT_PUBLIC_APP_URL }}/api/boletin/sync?secret=${{ secrets.BOLETIN_SYNC_SECRET }}" \
            | jq .
